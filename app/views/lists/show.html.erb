<h6><%= @list.title %></h6>

<table>
  <caption>Reasons</caption>
  <thead>
    <tr>
      <th>Body</th>
    </tr>
  </thead>

  <tbody>
    <% @list.reasons.each do |reason| %>
      <tr>
        <td><%= reason.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
