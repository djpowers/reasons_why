<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <span id="flashes">
      <% flash.each do |name, msg| -%>
        <%= content_tag :div, msg, class: "#{name} alert-box" %>
      <% end -%>
    </span>

    <%= link_to 'Sign Up', new_user_registration_path if !user_signed_in? %><br>
    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete if user_signed_in? %><br>
    <%= link_to 'Sign In', new_user_session_path if !user_signed_in? %><br>
    <%= link_to 'New List', new_list_path %>

    <%= yield %>
    <%= javascript_include_tag "application" %>
    <%= yield :extra_footer %>
  </body>
</html>
